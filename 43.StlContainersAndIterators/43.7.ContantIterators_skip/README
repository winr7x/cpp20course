Too easy

    std::vector<int> numbers{ 11,22,33,44,55,66,77};
    
    //Constant reverse iterators
    auto it1 = numbers.crbegin();                                         // NEW_FOR_ME crbegin()
    std::vector<int>::const_reverse_iterator it1= numbers.crbegin();      // vector::const_reverse_iterator

